apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-insta-front
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-insta-front
  template:
    metadata:
      labels:
        app: test-insta-front
    spec:
      containers:
      - name: test-insta-front
        image: mohamedsamir170/insta-front:test
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: insta-pay-config-test
            
        ports:
        - containerPort: 80

        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: test-insta-front
  namespace: testing
spec:
  type: LoadBalancer
  selector:
    app: test-insta-front
  ports:
    - port: 3000
      targetPort: 80
