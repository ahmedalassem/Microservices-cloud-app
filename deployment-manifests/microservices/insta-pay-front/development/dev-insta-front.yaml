apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-insta-front
  namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-insta-front
  template:
    metadata:
      labels:
        app: dev-insta-front
    spec:
      containers:
      - name: dev-insta-front
        image: mohamedsamir170/insta-front:dev
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: insta-pay-config-dev
            
        ports:
        - containerPort: 80

        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: dev-insta-front
  namespace: development
spec:
  type: LoadBalancer
  selector:
    app: dev-insta-front
  ports:
    - port: 3000
      targetPort: 80
