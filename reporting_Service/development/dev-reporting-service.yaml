apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-reporting-service
  namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-reporting-service
  template:
    metadata:
      labels:
        app: dev-reporting-service
    spec:
      containers:
      - name: dev-reporting-service
        image: mohamedsamir170/reporting-service:dev
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: insta-pay-config-dev
            
        ports:
        - containerPort: 5000

        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: dev-reporting-service
  namespace: development
spec:
  type: ClusterIP
  selector:
    app: dev-reporting-service
  ports:
    - port: 5000
      targetPort: 5000
